declare namespace windows {
    namespace mp {
      // Definiciones de funciones y objetos de RAGEMP aquí
      function trigger(eventName: string, ...args: any[]): void;
      function log(message: string): void;
      // ... otras funciones y objetos ...
    }
}
